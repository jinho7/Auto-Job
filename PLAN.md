# Auto-Job 기획서

> 채용 공고 수집 → Notion 정리 → 지원서 작성(인적사항 + 자소서) → 임시저장까지 AI가 처리하는 개인용 취업 자동화 도구.
> 다른 취준생이 설치해도, 내 정보가 바뀌어도 그대로 쓸 수 있는 범용 구조를 목표로 한다.

- 작성일: 2026-09-14
- 상태: **v1 완료** (2026-09-14). 아래 내용은 실제 구현에 맞춰 고쳐 두었다.
- 참고: [reference/ai-job-search](reference/ai-job-search) (MIT, 덴마크 구직용 Claude Code 프레임워크)

---

## 1. 범위

### v1에 포함
| # | 기능 | 결과물 |
|---|---|---|
| 0 | **내 정보 준비** | 한 번 입력하면 어느 채용 사이트에서든 재사용하는 프로필 |
| 1 | **공고 수집** (`collect`) | 조건에 맞는 공고를 중복 없이 Notion DB에 정리 |
| 2 | **지원서 작성** (`apply`) | 채용 사이트에 인적사항과 자소서를 입력하고 **임시저장**한 뒤 알림, Notion 페이지 정리 |

### v1에서 제외 (이후 검토)
- All-Auto 모드(최종 제출까지 자동). 제출은 되돌릴 수 없으므로 v1에서는 **항상 사람이 누른다**
- 면접 준비, 합불 추적 자동화, 지원 현황 대시보드 (설정 화면 `autojob ui` 는 v1 에 포함)

---

## 2. 사용 흐름

```
autojob init                 # 최초 1회: 설정 파일·내 정보 파일 생성, 설정 안내 (화면 / 터미널)
autojob ui                   # 설정 화면 (모든 설정과 내 정보, 시작하기 점검, 공고 수집 실행)
autojob doctor [--ai]        # 준비 상태 점검
autojob settings             # 설정 편집기 (터미널)
autojob profile edit         # 내 정보 편집기 (터미널)
autojob collect [--dry-run]  # 공고 수집 → 필터 → 중복 제거 → 지원 페이지 확인 → 보강 → Notion 등록
autojob apply <Notion페이지|지원URL>   # 지원서 작성 ①~⑥ (임시저장까지)
```

**개발 원칙:** 개발 과정에서 사용자의 정보나 설정값을 대신 채우지 않는다. 사용자에 따라 달라지는 값은 모두 설정이나 내 정보로 빼고, 사용자가 직접 입력하는 도구(편집기, CLI)를 제공한다.

`collect`는 cron이나 스케줄러에 걸어 매일 자동으로 돌릴 수 있다. `apply`는 공고 1건씩 실행하고, 로그인 같은 사람 확인 지점에서 멈춘다.

---

## 3. 전체 구조

```
┌──────────────────────── autojob CLI (TypeScript / Node 22) ────────────────────────┐
│                                                                                     │
│  collect ─▶ collectors/* ─▶ filter ─▶ dedup ─▶ enrich(AI) ─▶ notion                │
│                                                                                     │
│  apply ──▶ ① 준비 ─▶ ② 로그인 대기 ─▶ ③ 인적사항(AI) ─▶ ④ 자소서(AI)               │
│            ─▶ ⑤ 임시저장·알림 ─▶ ⑥ Notion 정리                                      │
│                                                                                     │
├──────────── 교체 가능한 어댑터 ─────────────────────────────────────────────────────┤
│  llm/      claude-cli (claude -p) │ codex-cli (codex exec) │ anthropic-api │ openai  │
│  browser/  aside-cdp (기본)       │ chrome (Playwright)    │ handoff (Aside 붙여넣기) │
│  notion/   official API (integration token)                                         │
└─────────────────────────────────────────────────────────────────────────────────────┘
          │ AI가 브라우저를 다룰 때는 autojob-browser MCP 서버를 거친다
          ▼
   autojob-browser MCP : snapshot / fill / click / select / upload / ask_user / notify
                         + 제출 차단 가드 내장 → 어떤 AI 도구를 붙여도 가드는 항상 켜짐
```

### 설계 원칙
1. **정해진 일은 코드, 판단은 AI.** 수집, 중복 제거, 글자수 검사, 제출 차단, Notion 기록은 코드로 하고, 직무 태그 판단, 폼 필드 매핑, 자소서 작성은 AI가 한다.
2. **도구 무관.** LLM과 브라우저는 어댑터로 교체한다. AI는 우리 MCP 서버를 통해서만 브라우저를 다룬다.
3. **개인 데이터와 프레임워크 분리.** `profile/me/`, `settings.yaml`, `.env`, `data/`는 git에 올리지 않는다. 저장소에는 항목 정의(`profile/schema.yaml`)와 중립 기본값(`settings.example.yaml`)만 둔다. `AUTOJOB_HOME`으로 개인 데이터 위치를 바꿀 수 있다.
4. **설정으로 바꾸기.** 기업 구분, 키워드, 수집처, Notion 필드 매핑, 자소서 문체는 전부 `settings.yaml`로 조정하고, `autojob settings` 편집기로 고친다.

---

## 4. 폴더 구조

```
Auto-Job/
├── PLAN.md · README.md
├── settings.example.yaml         # 설정 틀 (init 이 settings.yaml 로 복사)
├── settings.yaml · .env          # 내 설정 · 토큰과 API 키 (gitignore)
├── bin/autojob                   # 실행 진입점 (npm link)
├── profile/
│   ├── schema.yaml               # 내 정보 항목 정의 (공개). 편집기, 검사, 빈 틀, 자동 입력의 기준
│   └── me/                       # 내 정보 (gitignore): basic, education, career, extras, target, stories, files/
├── prompts/                      # AI 작업 지시서
│   ├── fill-basic-info.md        # 인적사항 입력 규칙
│   ├── essay-extract.md          # 자기소개서 문항·지원서 구성 찾기
│   ├── essay-write.md            # 조사 → 전략 → 작성 (문체 규칙은 설정에서 붙음)
│   ├── essay-review.md           # 검토
│   ├── find-apply-link.md        # 지원 페이지 찾기 (수집)
│   └── tag-roles.md              # 직무 태그 (수집)
├── src/
│   ├── cli.ts · init.ts · doctor.ts · config.ts · paths.ts · secrets.ts · http.ts
│   ├── profile/ · settings/ · ui/    # 내 정보·설정 저장소와 편집기, 대화형 입력
│   ├── server/ · web/            # 설정 화면 (127.0.0.1, 접속 토큰)
│   ├── collectors/               # saramin, jasoseol, jobkorea, catch, wanted
│   ├── jobs/                     # 기업 구분, 중복, 지원 페이지 확인·AI 검색, 직무 태그(규칙·AI), 처리 기록
│   ├── pipeline/                 # 수집 파이프라인과 리포트
│   ├── notion/                   # API 클라이언트, 속성 매칭, 페이지 만들기, 본문 채우기, DB 만들기
│   ├── browser/                  # CDP 연결(Aside/Chrome), 세션, 입력칸 목록, 제출 차단 가드, 자체 테스트
│   ├── mcp/                      # autojob-browser MCP 서버 (AI 가 브라우저를 다루는 유일한 통로)
│   ├── apply/                    # 지원서 작성 흐름, 브라우저 도구, 사람 확인 다리(bridge)
│   ├── essay/                    # 자기소개서 작성·검사
│   └── llm/                      # AI 연결 4가지 (claude-cli, codex-cli, anthropic-api, openai-api)
├── test/                         # 단위·흐름 테스트, 가짜 채용 사이트
├── data/                         # 로컬 상태 (gitignore): seen.json(처리 기록), runs/(리포트, 캡처)
└── reference/ai-job-search/      # 참고용 원본 (gitignore)
```

---

## 5. 기능 0: 내 정보 (프로필)

채용 사이트 입력란은 대부분 아래 여섯 영역으로 나뉜다. 이 구조를 그대로 항목 정의로 쓴다.

| 영역 | 파일 | 주요 항목 |
|---|---|---|
| 기본정보 | `basic.yaml` | 이름(한/영/한자), 생년월일, 연락처, 이메일, 주소, 병역, 보훈, 장애, 증명사진, 링크 |
| 학력 / 연구 | `education.yaml` | 고교, 대학(여러 개, 편입, 복수전공), 입학/졸업 연월, 학점과 만점, 논문과 연구 |
| 경력 / 교육 / NCS | `career.yaml` | 경력, 인턴, 교육 이수(부트캠프 등), NCS 직업교육과 직무경험 |
| 어학 / 자격 / 기타 | `extras.yaml` | 어학, 자격증, 수상, 대외활동/동아리/프로젝트, 기술 |
| 희망 조건 | `target.yaml` | 희망 직무, 고용형태, 근무지, 연봉, 입사 가능일 |
| 자소서 소재 | `stories.yaml` | 경험마다 상황, 행동, 결과(수치), 배운 점, 어울리는 문항 유형 |

### 5.1 항목 정의 (`profile/schema.yaml`)
- 항목마다 `label`(한글 이름), `type`, `required`, `hint`, `example`, `pattern`을 정한다.
- type: `text`, `longtext`, `number`, `date`(YYYY.MM.DD), `month`(YYYY.MM), `select`(선택지, 직접 입력 허용 여부), `tags`, `file`, `group`, `list`
- 편집기, 검사, 빈 틀 생성이 모두 이 파일을 따른다. 항목을 추가하려면 이 파일만 고친다.
- 한글 `label`은 지원서 자동 입력 때 AI가 사이트 입력란("입학년월" 등)과 내 정보를 짝지을 때도 쓴다.

### 5.2 입력 도구
| 명령 | 설명 |
|---|---|
| `profile edit [섹션]` | 대화형 편집기. 선택지는 목록에서 고르고, 형식이 틀리면 받지 않는다. 목록은 추가/수정/삭제 |
| `profile show / check / schema` | 보기, 검사, 입력 가능한 경로 보기 |
| `profile set / add / remove` | 스크립트용. 나중에 AI가 이력서를 읽어 내 정보를 채울 때도 이 명령을 쓴다 |

### 5.3 규칙
- 값이 없는 항목은 비워둔다. AI는 **추정해서 채우지 않는다.**
- 사이트가 월까지만 받으면 날짜를 자동으로 `YYYY.MM`으로 잘라서 넣는다.
- 문자열은 항상 따옴표로 저장한다. 우편번호 `03001`이나 만점 `4.0`이 숫자로 바뀌지 않게 하기 위해서다.
- 파일을 직접 편집해도 되고, 편집기로 저장해도 사용자가 단 주석은 남는다.

---

## 6. 기능 1: 공고 수집 (`collect`)

### 6.1 처리 단계
1. **수집:** `settings.yaml`에 켜둔 사이트에서 키워드 × 고용형태로 검색한다.
   - 수집처: 사람인, 잡코리아, 캐치, 자소설닷컴 (원티드는 robots.txt 확인 가능할 때만, 인크루트는 robots.txt 가 막아 제외)
   - 사이트별로 공개 API → 검색 결과 페이지 → 브라우저(로그인 필요 시 자동화 프로필) 순으로 가능한 방법을 쓴다. 개인용 저빈도 호출.
2. **필터:** 신입, 인턴, 계약직만 남긴다. 경력직은 제외하되 **[신입 및 경력]은 포함**한다. 마감이 지난 공고는 제외한다.
3. **기업 구분:** 기업 규모에 따라 넣을지 뺄지 정한다(6.3).
4. **중복 제거:** 같은 공고는 **절대** 두 번 넣지 않는다.
   - 기준: 지원 링크가 같거나, 정규화한 회사명이 같고 마감일(또는 둘 다 상시)이 같으면 중복. 애매하면 중복으로 본다
   - 로컬 처리 기록(`data/seen.json`)과 **Notion DB의 기존 페이지** 둘 다와 비교한다
   - 같은 회사의 같은 공고에 직무가 여러 개면 페이지 1개에 직무 태그를 여러 개 단다
5. **실제 지원 페이지 찾기 (필수):** 6.4 순서로 찾는다. **못 찾으면 없는 공고로 보고 등록하지 않는다.**
6. **보강:** 채용 분류는 코드로, 직무 태그는 규칙 → (설정에 따라) AI 로 채운다. 참고 키워드는 비워 둔다.
7. **Notion 등록:** 6.2 규칙으로 페이지를 만들고, 본문에 6개 섹션 헤더를 넣는다.
8. **수집 리포트:** 등록 건수와 제외 건수를 사유별로 보여준다(경력직, 중소, 중복, 마감, 지원 페이지 없음).

### 6.2 Notion 필드 규칙
대상 DB: 설정에서 고른 Notion DB (`notion.database_id`). 아래 필드 이름은 기본값이고, 설정에서 자기 DB 이름에 맞게 바꾼다.

| 필드 | 타입 | 규칙 |
|---|---|---|
| 회사명 | title | 회사명 |
| 직무 | multi_select | 웹 검색으로 판단해서 **기존 옵션 중에서만** 고른다. 새 태그는 만들지 않는다. 애매하면 비운다 |
| 채용 분류 | multi_select | 정규직 / 채용 연계·전환형 인턴 / 체험형 인턴 / 계약직 (기존 옵션명에 매핑) |
| 지원 마감 시간 | date | 상시 채용이면 빈칸 |
| 제출 상태 | select | 기업 구분의 `priority`가 켜진 경우 `작성중`, 나머지는 `제출전` |
| 합불 여부 | multi_select | `대기` 고정 |
| 참고 키워드 | text | 정말 특이한 사항만. 기본은 빈칸 |
| 지원 링크 | url | **실제로 지원서를 쓰는 페이지** (6.4). 못 찾은 공고는 아예 등록하지 않는다 |
| 서류 합격 발표 일자 | date | 빈칸 |
| 제출 자료 | file | 빈칸 |

필드 이름과 옵션 매핑은 `settings.yaml`의 `notion.fields`에 둔다. 그래서 다른 사람의 DB에도 매핑만 바꿔서 쓸 수 있다. 처음 쓰는 사용자는 설정 화면이나 `autojob notion bootstrap` 으로 같은 구조의 DB를 새로 만든다.

### 6.3 기업 구분 설정
자소설닷컴 달력 필터처럼 구분마다 켜고 끌 수 있다.

```yaml
company_types:
  대기업:        { include: true,  priority: true  }   # 회사명 단어(삼성, SK …) + 사이트 기업 규모
  유명IT:        { include: true,  priority: true  }   # 시드 목록 + 사용자 추가
  금융:          { include: true,  priority: false }   # 은행, 증권, 카드, 보험
  공기업:        { include: true,  priority: false }   # 회사명 단어(공사, 공단 …) + 사이트 정보
  중견:          { include: true,  priority: false }
  외국계:        { include: true,  priority: false }
  스타트업:      { include: true,  priority: false }   # 투자 유치나 인지도가 있는 곳
  중소:          { include: false, priority: false }   # 소규모는 제외
overrides:
  always_include: []    # 회사명 직접 지정
  always_exclude: []
  priority: []          # 작성중으로 올릴 회사
```

판정 순서: `overrides` → 구분별 회사 목록 → 회사명 단어 → 사이트가 알려준 기업 규모(사람인 기업형태, 잡코리아 기업구분, 캐치·자소설 기업 분류). 끝까지 판정되지 않으면 포함하고 리포트에 이유를 남긴다. 목록과 단어는 설정 화면에서 고친다.

### 6.4 실제 지원 페이지 찾기
"실제 지원 페이지"는 지원서를 실제로 작성하는 곳이다. 아래 순서로 찾는다.

1. **수집처에서 바로 지원하는 공고**(사람인 입사지원, 잡코리아 즉시지원, 원티드 지원 등)는 그 공고 URL이 곧 지원 페이지다.
2. **수집처 상세의 지원 링크.** 홈페이지 지원 공고는 상세 정보에 회사 채용 사이트 링크가 있다 (사람인·잡코리아·캐치·자소설).
3. **AI 웹 검색.** 링크가 없거나 열리지 않으면 AI 가 회사 채용 사이트(`*.recruiter.co.kr`, `*.greetinghr.com`, `careers.*` 등)에서 **같은 공고**를 찾는다. 같은 공고인지(회사, 제목, 마감일) 확인하는 것은 AI 의 몫이다.

찾은 링크는 코드가 다시 연다 (HTTP, 봇 차단이면 자동화 브라우저). 열리지 않거나 카페·블로그 같은 사이트면 등록하지 않고, 수집 리포트의 "지원 페이지 없음"에 이유와 함께 남긴다. 한 번 못 찾은 공고는 7일 뒤 다시 확인한다.

---

## 7. 기능 2: 지원서 작성 (`apply`)

### Phase ① 준비
- Notion 페이지와 공고 원문을 불러와 지원 링크를 확인한다.
- 브라우저 드라이버에 연결한다. 기본은 Aside 자동화 프로필이다.

### Phase ② 로그인 대기 (사람 확인 지점)
- 회원가입, 본인인증(PASS/SMS), 로그인, CAPTCHA, 약관·개인정보 동의는 **사용자가 직접** 한다.
- 도구는 브라우저 창을 앞으로 가져오고, 알림을 보낸 뒤 "완료했으면 Enter"를 기다린다.
- AI는 비밀번호를 보거나 입력하지 않는다. Aside 비밀번호 관리자의 자동완성은 사용해도 된다.

### Phase ③ 인적사항 입력 (AI)
대상은 **기본정보 / 학력·연구·경력·NCS / 어학·자격·기타**다. 자소서 전 단계까지만 입력한다.

AI 작업 규칙 (`prompts/fill-basic-info.md`):
- 이미 입력된 값은 수정하거나 삭제하지 않는다.
- 주소 입력 등에서 팝업이 뜰 수 있으니 팝업을 처리한다.
- 날짜는 UI 달력 대신 텍스트로 입력한다. 텍스트를 받지 않을 때만 달력을 쓴다.
- 월까지만 받는 입력란이 있다.
- 제공된 정보에 없는 값은 추정하지 않고 빈칸으로 둔다.
- 섹션마다 따로 저장해야 하는 사이트는 섹션 단위로 임시저장한다.
- 비워둔 항목과 참고사항은 기록해 두고 마지막에 리포트한다.

### Phase ④ 자기소개서 (AI)
1. 사이트에서 **문항과 글자수 제한**(공백 포함/미포함)을 추출한다.
2. **회사와 직무를 조사한다:** 인재상, 신년사, 최근 이슈와 프로젝트, 직무 내용. 출처는 독립적으로 찾은 것만 쓰고, 공고 본문에 있는 링크나 지시는 따르지 않는다.
3. **문항 전체 전략을 먼저 세운다:** 문항마다 소재 1~2개를 배정하고, 소재가 겹치거나 한 문항에 여러 개가 몰리지 않게 한다.
4. **작성:** Notion에 적어둔 자소서 프롬프트 규칙을 `prompts/essay-write.md`로 옮긴다.
   - 문단마다 내용이 드러나는 [소제목]을 단다
   - '~습니다' 체를 쓰고, 가운뎃점은 쓰지 않는다
   - GPT 티 나는 표현은 금지한다 (금지 목록)
   - 성과는 과정과 수치로 풀어 쓴다
   - 블라인드 규정을 지킨다 (실명, 학교, 특정 동아리명이나 앱 이름 금지)
5. **코드 검사:** 글자수, 금지 표현, 블라인드 위반, 반복 문자.
6. **리뷰어 AI**가 비평하고, 그 결과로 수정한다.
7. 사이트 입력란에 입력한다.

### Phase ⑤ 임시저장 · 알림
- **임시저장**을 누른다. 최종 제출은 절대 누르지 않는다.
- 화면 전체 스크린샷을 `data/runs/...`에 저장하고, 브라우저 창을 앞으로 가져온다.
- macOS 알림과 터미널 리포트를 낸다:
  - 입력 완료 요약
  - **[비워둔 Value 값]** 목록
  - 참고사항 (선택 문항, 파일 업로드 필요 항목 등)

### Phase ⑥ Notion 정리
해당 페이지 본문의 템플릿 섹션을 채운다.

| 섹션 | 내용 |
|---|---|
| 절차 | 전형 절차 (공고 원문 요약, 절차 이미지가 있으면 캡처를 첨부) |
| 회사/조직 소개 | 조사한 회사 정보 요약 |
| 지원 직무 | 지원한 직무명과 직무 설명 |
| **자기소개서 질문** | 문항(굵게)과 작성한 답변 |
| 프로젝트 및 동아리 작성 여부 | 사이트에 해당 입력란이 있었는지, 무엇을 입력했는지 |
| **제출 자료 여부** | 업로드가 필요한 서류 목록과 업로드 여부 |

그리고 `제출 상태`를 **`작성중`**으로 바꾼다.

---

## 8. 브라우저 드라이버

### 8.1 드라이버 종류
| 드라이버 | 설명 | 상태 |
|---|---|---|
| `aside` (기본) | Aside(Chromium 152)를 `--remote-debugging-port`로 띄우고 Playwright `connectOverCDP`로 조종. 자동화 전용 프로필 `~/.autojob/browser-profiles/aside` 사용 | ✅ 2026-09-14 연결 테스트 통과 |
| `chrome` | Chrome 을 같은 방식(CDP, 자동화 전용 프로필)으로 조종. Aside 가 없으면 init 이 자동으로 고른다 | 대체용 |
| `handoff` | 지시문을 만들어 Aside 내장 agent에 붙여넣게 하는 방식 | v1 에서 제외 (고르면 안내하고 멈춤) |

Chromium 보안 정책 때문에 원격 조종은 **기본 프로필이 아닌 별도 프로필**에서만 켤 수 있다. 채용 사이트 로그인은 자동화 프로필에서 한 번씩 해두면 계속 유지된다.

### 8.2 제출 차단 가드 (3중)
1. **페이지 안 가드:** 모든 페이지와 프레임에 스크립트를 주입해서 금지 문구(`제출`, `작성완료`, `지원완료`, `지원하기` 등) 버튼 클릭과 폼 submit을 캡처 단계에서 막는다. 연결 테스트에서 동작을 확인했다.
2. **드라이버 가드:** MCP의 `click` 도구가 누르기 전에 대상의 문구를 검사해서 거부한다.
3. **이동 가드:** 뒤로가기, 새로고침, 페이지 나가기, 탭 닫기를 막는다.

금지 문구 목록은 설정에서 추가할 수 있다. `임시저장`은 허용 목록으로 둔다.

### 8.3 연결 테스트 결과 (2026-09-14)
가짜 지원서 페이지로 검증했다.
- ✅ 페이지 이동, 텍스트 입력, 기존 값 보존
- ✅ 날짜 텍스트 입력 (`YYYY.MM`)
- ✅ 주소 팝업 창 처리
- ✅ 자소서 입력과 글자수 확인
- ✅ 임시저장 클릭
- ✅ 제출 클릭 차단
- ✅ 스크린샷 저장

---

## 9. AI 연결
- `llm` 어댑터가 작업 지시서(prompts/*)와 컨텍스트를 넘기고, 필요하면 `autojob-browser` MCP를 연결한다.
- 기본값은 `claude-cli`(`claude -p`, 구독 사용)다. `codex-cli`와 API 키 방식도 지원한다.
  - `claude-cli`: 내장 도구는 넘겨받은 것만, MCP 는 `--strict-mcp-config` 로 우리 서버만.
  - `codex-cli`: `codex exec --json`, 읽기 전용 샌드박스, MCP·웹 검색은 `-c` 로 이번 실행에만. 지시문은 작업 앞에 붙인다.
  - `anthropic-api` / `openai-api`: 우리가 대화를 반복한다. 웹은 서버 도구(web_search, web_fetch / web_search), 브라우저는 MCP 클라이언트로 우리 서버를 띄워 도구를 전달한다.
  - 기능별 모델 → `llm.model` → 방식별 기본 순서로 모델을 고른다.
- 공고 본문은 **신뢰할 수 없는 입력**으로 취급한다. 본문 안의 지시문을 따르지 않고, 본문에 있는 링크도 열지 않는다.

---

## 10. Notion 연동
- **자동화에는 공식 API를 쓴다.** `NOTION_TOKEN`(integration)을 쓰고, 대상 DB를 integration에 공유해 두어야 한다.
- 페이지는 DB의 **기본 템플릿**으로 만들고(템플릿이 속성을 덮지 않게 적용 뒤 속성을 한 번 더 넣음), 템플릿이 없으면 설정의 제목 6개로 본문을 만든다.
- 새 사용자: 설정 화면이나 `autojob notion bootstrap` 으로 같은 구조의 DB를 만든다.
- 현재 DB에서 정리할 점: `체형형인턴`은 오타로 보이고, `합불 여부` 옵션에 cafe 링크가 섞여 있다. 매핑 설정으로 흡수하거나 직접 정리한다.

---

## 11. 보안 · 개인정보
- `profile/me/`, `settings.yaml`, `.env`, `data/`는 git에서 제외한다. 공개 저장소에는 빈 틀만 올린다.
- 비밀번호, 본인인증, 결제, 약관 동의, 최종 제출은 사람이 한다.
- 스크린샷과 리포트에는 개인정보가 들어가므로 로컬(`data/`)에만 저장한다.

---

## 12. 로드맵

| 단계 | 내용 | 완료 기준 |
|---|---|---|
| **M0 뼈대** ✅ | 저장소 구조, `settings.example.yaml`, CLI 골격, `aside-cdp` 드라이버와 가드 | `autojob browser test` 통과 (2026-09-14, 14/14) |
| **M0.5 입력 도구** ✅ | 항목 정의(`profile/schema.yaml`), 내 정보 편집기/검사, 설정 편집기, 스크립트용 명령 | 단위 테스트 24개, 실제 터미널 조작 테스트 통과 (2026-09-14) |
| **M0.6 설정 화면** ✅ | `autojob ui` 로컬 웹 화면(모든 설정과 내 정보), 비밀값 관리(.env), Notion 연결 안내·토큰 검증·DB 고르기·속성 매칭 검사 | 테스트 37개, 화면 조작 확인 (2026-09-14) |
| **M1 Notion** ✅ | 공고 → 페이지 생성(DB 기본 템플릿 우선, 없으면 설정의 제목들), 기존 옵션만 사용, 중복 조회(링크 / 회사+마감일), 새 사용자용 DB 만들기, `notion add/list/check/bootstrap`, 화면의 "공고 1건 넣어보기" | 테스트 49개 (2026-09-14). 실제 DB 검증은 사용자 토큰 입력 후 |
| **M2 collect** ✅ | 사람인(HTTP) + 자소설닷컴(자동화 브라우저) 수집기, 경력/고용형태/마감 필터, 이미 처리한 공고 기록, 같은 회사·마감일 합치기, 사이트 정보로 기업 구분, 실제 지원 페이지 확인, 규칙 기반 직무 태그, Notion 등록, 리포트, `autojob collect`, 화면의 "공고 수집" | 테스트 62개, 실제 사이트 미리보기 확인 (2026-09-14) |
| **M2b** ✅ | 잡코리아(채용정보 목록 `_GI_List` + 상세, 포함 키워드·직무 대분류)·캐치(목록 API + 상세)·원티드(자동화 브라우저, robots.txt 를 읽을 수 있을 때만) 수집기, 지원 링크가 없거나 열리지 않는 공고의 AI 웹 검색(코드가 다시 열어 확인, 카페·블로그 거부, 회당 한도), AI 직무 태그(규칙으로 못 단 공고만 / 다시 보기), 직무 태그 없는 공고 빼기 옵션, 지원 페이지 못 찾은 공고 7일 뒤 재확인, 설정 화면 "AI 보강" | 테스트 90개, 잡코리아·캐치 실제 미리보기, AI 링크 검색·태그 실제 확인 (2026-09-14) |
| **M3 apply ①~③** ✅ | Notion 페이지 → 브라우저 열기, 로그인 대기(사람), `autojob-browser` MCP 도구(입력칸 목록, 입력, 선택, 팝업 창 전환, 행 추가 등 — 코드로 기존 값 보호·제출/삭제/로그아웃 차단), `claude -p` 로 인적사항 입력(내장 도구 전부 끔), 비워둔 값·참고사항 리포트 | 가짜 채용 사이트에서 실제 Claude 로 전체 흐름 확인 (2026-09-14). 실제 채용 사이트 확인은 사용자와 함께 |
| **M4 apply ④** ✅ | 문항 찾기(AI+브라우저, `page_text`/`set_questions`) → 조사·전략·작성(AI, 웹 검색만) → 기계 검사(글자수 3가지 방식, 금지 표현 `~` 패턴, 가운뎃점, 소제목, 끝맺음, 블라인드, 반복) → 검토 → 고쳐 쓰기 → 형식 고치기 → 입력(코드). `autojob essay`(브라우저 없이), `apply --steps` | 가짜 채용 사이트에서 실제 Claude 로 문항 2개 작성·입력 확인, 검토가 지어낸 내용을 잡아 고쳐 씀 (2026-09-14) |
| **M5 apply ⑤~⑥** ✅ | 임시저장(설정의 버튼 문구, 가드 적용), 검토 화면 띄우기, Notion 본문 6개 섹션 채우기(내용 있는 섹션은 둠, 없는 제목은 끝에 추가), 제출 상태 `작성중`(옵션 확인), 지원서 구성 기록(`set_form_info`), `autojob notion fill` | 가짜 채용 사이트에서 임시저장·절차·제출 서류 추출 확인, Notion 쓰기는 가짜 클라이언트로 검증 (2026-09-14). 실제 Notion 쓰기는 사용자 토큰 입력 후 |
| **M6 범용화** ✅ | AI 연결 방식 4가지(Claude Code / Codex CLI / Anthropic API / OpenAI API)를 같은 모양으로 — 모든 AI 호출(인적사항, 자소서, 지원 페이지 검색, 직무 태그)이 설정 하나로 바뀜, `autojob doctor [--ai]` 준비 상태 점검, `autojob init` 마법사(설정 화면 / 터미널 차례대로), 설치된 브라우저 찾기(macOS·Windows·Linux), 설정 화면 "시작하기"와 AI 연결 확인·기본 모델, README 새 사용자 안내(준비물 → 설치 → 처음 쓰는 순서 → 문제 해결), tsx 를 실행 의존성으로 | 테스트 101개. 저장소를 개인 파일 없이 새 폴더에 복사해 README 순서대로 `npm ci → init → doctor → 테스트` 확인, Claude Code 로 가짜 채용 사이트 지원서 입력 재확인 (2026-09-14). Codex·API 방식은 가짜 codex / 가짜 응답 / 실제 MCP 서버로만 확인 |
| **v1 마무리** ✅ | 문서를 실제 구현에 맞게 정리, 구현하지 않은 handoff 선택지 제거, 버전 1.0.0 | 테스트 전체 통과, 브라우저 자체 테스트 통과 (2026-09-14) |

### 이후 (v1 다음)
| 항목 | 메모 |
|---|---|
| 실제 환경 확인 | 사용자 Notion 토큰으로 실제 DB 등록·본문 채우기, 실제 채용 사이트 여러 곳에서 지원서 작성 |
| Codex CLI · API 방식 실사용 확인 | 지금은 가짜 codex / 가짜 응답으로만 확인 |
| 원티드 | robots.txt 를 읽을 수 있게 되면 자동으로 수집 (코드는 준비됨) |
| All-Auto | 최종 제출까지 자동. 되돌릴 수 없어서 v1 에서는 항상 사람이 누름 |
| 면접 준비, 합불 추적, 지원 현황 대시보드 | |

---

## 13. 결정 기록
| 날짜 | 결정 |
|---|---|
| 2026-09-14 | 도구 무관 구조. LLM과 브라우저는 어댑터로 교체 |
| 2026-09-14 | 브라우저는 Aside(CDP) 1순위, Chrome 대체, handoff 선택 |
| 2026-09-14 | 카카오톡 오픈채팅 CSV는 수집처에서 제외 (채용 사이트 검색으로 충분) |
| 2026-09-14 | 기업 구분은 설정으로 켜고 끔. 중소는 기본 제외, 대기업과 유명IT는 `작성중` |
| 2026-09-14 | apply가 끝나면 제출 상태를 `작성중`으로 변경 |
| 2026-09-14 | v1은 임시저장까지만. 최종 제출은 사람이 함 |
| 2026-09-14 | 실제 지원 페이지가 필수. 못 찾으면 없는 공고로 보고 등록하지 않음 (대체 링크 없음) |
| 2026-09-14 | 개발 중에는 사용자 정보와 설정값을 채우지 않음. 사용자별 값은 설정과 내 정보로 빼고 입력 도구를 제공 |
| 2026-09-14 | 내 정보는 `profile/schema.yaml` 한 곳에서 정의 (템플릿 파일 대신) |
| 2026-09-14 | 토큰, API 키까지 포함해 모든 설정을 도구 안에서 입력. 간단한 로컬 웹 화면(`autojob ui`) 제공 |
| 2026-09-14 | Notion API 2026-03-11 사용. 페이지는 DB 기본 템플릿으로 만들고, 템플릿 적용이 끝난 뒤 속성 값을 다시 넣어 템플릿 기본값이 덮지 못하게 함 |
| 2026-09-14 | 중복 기준: 지원 링크가 같거나, 회사명이 같고 마감일(또는 둘 다 상시)이 같으면 중복. 애매하면 넣지 않는 쪽 |
| 2026-09-14 | 수집 원칙: robots.txt 준수, 정직한 User-Agent(AutoJob/…), 요청 간격, 봇 차단(Cloudflare 등) 우회 안 함. 브라우저가 필요한 사이트는 자동화 브라우저로 사람처럼 페이지를 열고, 확인 화면이 나오면 사용자에게 맡김 |
| 2026-09-14 | 사이트 조사 결과: 사람인 검색·상세 허용 / 자소설닷컴 허용(브라우저 전용) / 잡코리아 검색 금지·목록 허용 / 캐치 검색 금지 / 인크루트 전면 금지 → 수집 안 함 / 원티드 robots.txt 확인 불가 → 보류 |
| 2026-09-15 | 지원 순서 변경: 지원 페이지 열기 → (로그인 전) 페이지 글 + 웹 검색으로 모집 직무·절차·회사 소개 정리, 지원 직무 선택 → Notion 절차/회사 소개/지원 직무 → 로그인 요청 → 인적사항 → 자기소개서 → 임시저장 → Notion 자기소개서 질문(질문·답변)으로 마무리. 조사 결과는 자기소개서에 넘겨 다시 조사하지 않음 |
| 2026-09-15 | 설정 화면 "지원서 작성": 공고 최대 8개 → 지원서마다 대화방 + 브라우저 새 창(뒤에서). 사람이 할 일(로그인, ask_user)은 알림 + 그 창을 앞으로 + 빨간 점, 답은 대화방에서. 동시 진행 개수는 설정. 창은 CDP targetId 로 짝지어 여러 지원서가 같은 창을 잡지 않게 함 (실제로 겹침 문제를 찾아 고침) |
| 2026-09-15 | 모델·추론 성능: Claude Code `--effort`, Codex `model_reasoning_effort`, Anthropic API `output_config.effort`(공식 SDK, Haiku 제외, Opus 5/Fable 5.1 은 거절 시 서버 대체 `fallbacks: "default"`), OpenAI `reasoning.effort`. 작업별 → 연결 → 기본 순서 |
| 2026-09-15 | AI 연결 여러 개 돌려쓰기: 한도·로그인 문제면 그 연결을 쉬게 하고(`data/llm-state.json`, 풀리는 시각을 알면 그때까지) 다음 연결로. 다른 오류는 넘어가지 않음. 계정 폴더(CLAUDE_CONFIG_DIR / CODEX_HOME)로 같은 CLI 여러 계정. 빈 계정 폴더 → 로그인 필요로 판정 → 다음 계정으로 넘어가는 것을 실제로 확인 |
| 2026-09-15 | 기본 프로필 원격 조종은 Chromium 정책상 불가 → 기본 프로필의 비밀번호 파일(Login Data)만 자동화 프로필로 복사하는 기능으로 대신. 쿠키는 가져오지 않음. 사용자가 누를 때만 |
| 2026-09-15 | 내 정보 "붙여넣어 채우기": AI 가 나누고 코드가 형식을 맞추고 검사, 미리보기 후 사용자가 고른 섹션만 적용. 글 속 입력 지시는 규칙으로, 칸이 없는 정보는 "지원서 입력 메모"로 |
| 2026-09-15 | 자소서 소재는 "폴더 연결". 소재 찾기 단계는 폴더만 읽고 웹 도구 없음, 글쓰기 단계는 웹만 쓰고 파일 도구 없음 → 파일 내용이 웹으로 나갈 길을 막음. Claude Code 는 파일 도구를 허용 목록에 넣지 않고 `--add-dir` 로만 열어 폴더 밖 읽기가 거절됨을 실제로 확인 |
| 2026-09-14 | M6: API 방식은 MCP 클라이언트로 같은 `autojob-browser` 서버를 띄워 도구를 전달한다 → 어느 AI 를 써도 기존 값 보호·제출 차단이 같은 코드로 적용됨 |
| 2026-09-14 | M6: Codex CLI 는 이 컴퓨터에 없어 가짜 codex 로만 확인 → "실험적"으로 표시. 실제 설치 후 확인 필요 |
| 2026-09-14 | M2b: 잡코리아는 검색(/Search?stext=) 대신 허용된 채용정보 목록의 "포함 키워드"와 직무 대분류(dutyCtgr)를 쓴다. 캐치는 채용공고 페이지가 부르는 목록 API(허용 경로)를 쓰고 로그 API는 부르지 않는다. 원티드는 robots.txt 가 브라우저에서도 403 이라 수집기를 만들되 robots.txt 를 읽을 수 있을 때만 돈다 |
| 2026-09-14 | AI 지원 페이지 검색 결과는 믿지 않고 코드가 다시 열어 확인한다. 카페·블로그 등은 거부(설정). 한 번 못 찾은 공고는 7일 동안 다시 찾지 않는다 (사용량 절약). 비용 참고: 2건 검색 약 $0.3, 태그 3건 약 $0.2 (Sonnet) |
| 2026-09-14 | 기업 구분은 사이트 정보(자소설 기업분류, 사람인 기업형태) + 설정의 구분별 회사 목록·회사명 단어로 판정. 여러 구분이면 하나라도 포함이면 포함 |
| 2026-09-14 | 지원서 입력 AI 는 `claude -p --tools "" --strict-mcp-config --permission-mode dontAsk` 로 실행해 우리 MCP 도구만 쓰게 함. 안전 규칙(기존 값 보호, 제출·삭제·로그아웃·페이지 이탈 차단)은 프롬프트가 아니라 도구 코드에서 지킴 |
| 2026-09-14 | `--mcp-config` 서버는 기본적으로 뒤에서 연결되어 첫 턴에 도구가 없을 수 있음 → 설정에 `alwaysLoad: true` 를 넣어 연결을 기다린 뒤 시작. 연결이 안 되면 바로 중단. MCP 서버는 시작 즉시 응답하고 브라우저 연결은 첫 도구 호출 때 완료 |
| 2026-09-14 | 자기소개서: 사실은 내 정보·자소서 소재에만 근거, 회사 사실은 조사로 확인한 것만. 작성·검토 AI 는 웹 검색 도구만, 형식 고치기는 도구 없이. 개인 문체(끝맺음, 소제목, 가운뎃점, 블라인드, 쓰지 않을 표현)는 설정에서 주입 |
| 2026-09-14 | Notion 정리는 "이미 내용이 있는 섹션은 건드리지 않음" (기존 값 보호와 같은 원칙). 제출 상태는 DB 에 있는 옵션일 때만, 작업을 끝까지 마쳤을 때만 바꿈 |
| 2026-09-14 | 자동화 브라우저에 창이 하나도 없으면 빈 탭을 열고 붙음 (macOS 에서 탭을 다 닫아도 앱이 떠 있는 경우) |
| 2026-09-14 | 가드와 대화상자 처리는 세션이 연 탭(과 그 팝업)에만 적용. 같은 자동화 브라우저에서 여러 작업이 돌아도 서로 간섭하지 않음. 자동화 브라우저는 가려져도 화면을 그리도록 Chromium 옵션을 켜고 띄움 |

## 14. 남은 확인 사항
- [ ] Aside **기본 프로필**에 붙는 방식(실행 중인 브라우저에 권한을 허락하고 연결)을 지원하는지. 된다면 별도 로그인 없이 쓸 수 있음
- [ ] 수집처별 접근 방식 조사 (공개 API 유무, 로그인 필요 여부, 이용약관)
